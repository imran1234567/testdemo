import React, { Component } from 'react';
import "./footer.css"

class Footer extends Component {
    render() {
        return (
            <footer>
                Footer Section
            </footer>
        );
    }
}

export default Footer;
